
type serverType = {
  id: number,
  code: string,
  flag: string,
  url: string,
  active: boolean,
  speed_active: boolean,
  classic_active: boolean,
  locale: string,

  world_cnt: number,
}

export type {serverType}
